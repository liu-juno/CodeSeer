# 列表分页展示 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为项目管理、迭代管理、需求管理三个列表页面添加后端分页支持，响应格式统一为 `{ items: [], total, page, page_size }`

**Architecture:** 后端新增 `PaginatedResponse` 泛型结构，改写 `list_projects`、`list_iterations`、`list_requirements` 三个接口支持分页参数。前端封装 `usePagination` composable，改造三个列表页添加 `el-pagination` 组件。

**Tech Stack:** Python (FastAPI + SQLAlchemy), Vue 3 + Element Plus, TypeScript

---

## Global Constraints

- 分页默认值: `page=1`, `page_size=20`
- 响应格式: `{ items: List, total: int, page: int, page_size: int }`
- 前端使用 Element Plus `el-pagination` 组件
- 设计文档路径: `docs/cs/codeseer/sprint-1/项目管理迭代管理需求管理列表分页展示/specs/`

---

## Task 1: 后端 — 新增 PaginatedResponse 泛型结构

**Files:**
- Modify: `backend/app/schemas/schemas.py`

**Interfaces:**
- Produces: `PaginatedResponse[T]` — 泛型分页响应模型

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/test_pagination.py (新文件)
from app.schemas.schemas import PaginatedResponse

def test_paginated_response_structure():
    response = PaginatedResponse(items=[], total=0, page=1, page_size=20)
    assert response.items == []
    assert response.total == 0
    assert response.page == 1
    assert response.page_size == 20
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && python -m pytest tests/test_pagination.py::test_paginated_response_structure -v`
Expected: ERROR — module 'app.schemas.schemas' has no attribute 'PaginatedResponse'

- [ ] **Step 3: 实现 PaginatedResponse**

在 `backend/app/schemas/schemas.py` 末尾添加:

```python
from typing import Generic, TypeVar, List
from pydantic import BaseModel

T = TypeVar("T")

class PaginatedResponse(BaseModel, Generic[T]):
    items: List[T]
    total: int
    page: int
    page_size: int
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && python -m pytest tests/test_pagination.py::test_paginated_response_structure -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/schemas/schemas.py backend/tests/test_pagination.py
git commit -m "feat: add PaginatedResponse generic schema"
```

---

## Task 2: 后端 — 改造 list_projects 接口

**Files:**
- Modify: `backend/app/api/projects.py`

**Interfaces:**
- Consumes: `PaginatedResponse[ProjectResponse]`
- Produces: `GET /projects?page=1&page_size=20`

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/test_projects_pagination.py (新文件)
from fastapi.testclient import TestClient

def test_list_projects_pagination(client: TestClient):
    # 创建 25 个项目（默认 page_size=20）
    for i in range(25):
        client.post("/projects", json={"name": f"Project-{i}", "identifier": f"project-{i}"})

    # 第一页
    res = client.get("/projects?page=1&page_size=20")
    assert res.status_code == 200
    data = res.json()
    assert "items" in data
    assert "total" in data
    assert "page" in data
    assert "page_size" in data
    assert data["total"] == 25
    assert len(data["items"]) == 20
    assert data["page"] == 1
    assert data["page_size"] == 20

    # 第二页
    res2 = client.get("/projects?page=2&page_size=20")
    data2 = res2.json()
    assert len(data2["items"]) == 5
    assert data2["page"] == 2
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && python -m pytest tests/test_projects_pagination.py::test_list_projects_pagination -v`
Expected: FAIL — 响应不是分页格式

- [ ] **Step 3: 实现分页逻辑**

在 `projects.py` 中：

```python
from app.schemas.schemas import ProjectCreate, ProjectUpdate, ProjectResponse, PaginatedResponse

@router.get("", response_model=PaginatedResponse[ProjectResponse])
async def list_projects(
    page: int = Query(1, ge=1),
    page_size: int = Query(20, ge=1, le=100),
    db: AsyncSession = Depends(get_db),
):
    # COUNT
    count_result = await db.execute(select(func.count()).select_from(Project))
    total = count_result.scalar() or 0

    # 数据
    offset = (page - 1) * page_size
    result = await db.execute(
        select(Project)
        .order_by(Project.created_at.desc())
        .offset(offset)
        .limit(page_size)
    )
    items = result.scalars().all()

    return PaginatedResponse(items=items, total=total, page=page, page_size=page_size)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && python -m pytest tests/test_projects_pagination.py::test_list_projects_pagination -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/api/projects.py backend/tests/test_projects_pagination.py
git commit -m "feat: add pagination to list_projects endpoint"
```

---

## Task 3: 后端 — 改造 list_iterations 接口

**Files:**
- Modify: `backend/app/api/iterations.py`

**Interfaces:**
- Consumes: `PaginatedResponse[IterationResponse]`
- Produces: `GET /iterations?page=1&page_size=20`

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/test_iterations_pagination.py (新文件)
from fastapi.testclient import TestClient

def test_list_iterations_pagination(client: TestClient, sample_project):
    # 创建 25 个迭代
    for i in range(25):
        client.post("/iterations", json={
            "name": f"Iter-{i}",
            "project_id": str(sample_project["id"])
        })

    res = client.get("/iterations?page=1&page_size=20")
    assert res.status_code == 200
    data = res.json()
    assert data["total"] == 25
    assert len(data["items"]) == 20
    assert data["page"] == 1

    res2 = client.get("/iterations?page=2&page_size=20")
    data2 = res2.json()
    assert len(data2["items"]) == 5
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && python -m pytest tests/test_iterations_pagination.py::test_list_iterations_pagination -v`
Expected: FAIL

- [ ] **Step 3: 实现分页逻辑**

```python
from app.schemas.schemas import IterationCreate, IterationUpdate, IterationResponse, PaginatedResponse
from sqlalchemy import func

@router.get("", response_model=PaginatedResponse[IterationResponse])
async def list_iterations(
    page: int = Query(1, ge=1),
    page_size: int = Query(20, ge=1, le=100),
    db: AsyncSession = Depends(get_db),
):
    count_result = await db.execute(select(func.count()).select_from(Iteration))
    total = count_result.scalar() or 0

    offset = (page - 1) * page_size
    result = await db.execute(
        select(Iteration)
        .order_by(Iteration.created_at.desc())
        .offset(offset)
        .limit(page_size)
    )
    items = result.scalars().all()

    return PaginatedResponse(items=items, total=total, page=page, page_size=page_size)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && python -m pytest tests/test_iterations_pagination.py::test_list_iterations_pagination -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/api/iterations.py backend/tests/test_iterations_pagination.py
git commit -m "feat: add pagination to list_iterations endpoint"
```

---

## Task 4: 后端 — 改造 list_requirements 接口

**Files:**
- Modify: `backend/app/api/requirements.py`

**Interfaces:**
- Consumes: `PaginatedResponse[RequirementResponse]`
- Produces: `GET /requirements?page=1&page_size=20`

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/test_requirements_pagination.py (新文件)
from fastapi.testclient import TestClient

def test_list_requirements_pagination(client: TestClient, sample_project):
    # 创建 25 个需求
    for i in range(25):
        client.post("/requirements", json={
            "title": f"Req-{i}",
            "project_id": str(sample_project["id"]),
            "priority": "P2"
        })

    res = client.get("/requirements?page=1&page_size=20")
    assert res.status_code == 200
    data = res.json()
    assert data["total"] == 25
    assert len(data["items"]) == 20
    assert data["page"] == 1
    assert data["page_size"] == 20

    res2 = client.get("/requirements?page=2&page_size=20")
    data2 = res2.json()
    assert len(data2["items"]) == 5
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && python -m pytest tests/test_requirements_pagination.py::test_list_requirements_pagination -v`
Expected: FAIL

- [ ] **Step 3: 实现分页逻辑**

在 `requirements.py` 中改动 `list_requirements`:

```python
from sqlalchemy import func

@router.get("", response_model=PaginatedResponse[RequirementResponse])
async def list_requirements(
    project_id: Optional[UUID] = Query(None),
    iteration_id: Optional[UUID] = Query(None),
    assignee_id: Optional[UUID] = Query(None),
    status: Optional[str] = Query(None),
    page: int = Query(1, ge=1),
    page_size: int = Query(20, ge=1, le=100),
    db: AsyncSession = Depends(get_db),
):
    # 构建基础 query
    query = select(Requirement)
    count_query = select(func.count()).select_from(Requirement)

    if project_id:
        query = query.where(Requirement.project_id == str(project_id))
        count_query = count_query.where(Requirement.project_id == str(project_id))
    if iteration_id:
        query = query.where(Requirement.iteration_id == str(iteration_id))
        count_query = count_query.where(Requirement.iteration_id == str(iteration_id))
    if assignee_id:
        query = query.where(Requirement.assignee_id == str(assignee_id))
        count_query = count_query.where(Requirement.assignee_id == str(assignee_id))
    if status:
        query = query.where(Requirement.status == status)
        count_query = count_query.where(Requirement.status == status)

    # COUNT
    total = (await db.execute(count_query)).scalar() or 0

    # 数据
    offset = (page - 1) * page_size
    result = await db.execute(
        query.order_by(Requirement.created_at.desc()).offset(offset).limit(page_size)
    )
    items = result.scalars().all()

    return PaginatedResponse(items=items, total=total, page=page, page_size=page_size)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && python -m pytest tests/test_requirements_pagination.py::test_list_requirements_pagination -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/api/requirements.py backend/tests/test_requirements_pagination.py
git commit -m "feat: add pagination to list_requirements endpoint"
```

---

## Task 5: 前端 — 新增 usePagination Composable

**Files:**
- Create: `frontend/src/composables/usePagination.ts`

**Interfaces:**
- Produces: `usePagination<T>(fetcher)` — 返回 `{ items, total, page, pageSize, loading, fetchPage, onPageChange, onSizeChange }`

- [ ] **Step 1: 写失败测试**

```typescript
// frontend/src/tests/usePagination.test.ts (新文件)
import { describe, it, expect, vi } from 'vitest'

describe('usePagination', () => {
  it('should initialize with correct defaults', async () => {
    const fetcher = vi.fn().mockResolvedValue({ items: [], total: 0 })
    const { page, pageSize, total } = usePagination(fetcher)

    expect(page.value).toBe(1)
    expect(pageSize.value).toBe(20)
    expect(total.value).toBe(0)
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd frontend && npm test -- --run src/tests/usePagination.test.ts`
Expected: ERROR — usePagination not defined

- [ ] **Step 3: 实现 usePagination**

```typescript
// frontend/src/composables/usePagination.ts
import { ref } from 'vue'

export function usePagination<T>(
  fetcher: (page: number, pageSize: number) => Promise<{ items: T[]; total: number }>
) {
  const items = ref<T[]>([])
  const total = ref(0)
  const page = ref(1)
  const pageSize = ref(20)
  const loading = ref(false)

  const fetchPage = async (p: number) => {
    loading.value = true
    try {
      const res = await fetcher(p, pageSize.value)
      items.value = res.items
      total.value = res.total
      page.value = p
    } finally {
      loading.value = false
    }
  }

  const onPageChange = (p: number) => fetchPage(p)
  const onSizeChange = (size: number) => {
    pageSize.value = size
    fetchPage(1)
  }

  return { items, total, page, pageSize, loading, fetchPage, onPageChange, onSizeChange }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd frontend && npm test -- --run src/tests/usePagination.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add frontend/src/composables/usePagination.ts frontend/src/tests/usePagination.test.ts
git commit -m "feat: add usePagination composable"
```

---

## Task 6: 前端 — 改造 Projects.vue

**Files:**
- Modify: `frontend/src/views/Projects.vue`

**Interfaces:**
- Consumes: `usePagination`, `projectsApi`
- Produces: 分页后的项目列表

- [ ] **Step 1: 写失败测试**

```typescript
// frontend/src/tests/Projects.pagination.test.ts (新文件)
import { describe, it, expect, vi } from 'vitest'
import { flushPromises } from '@vue/test-utils'

describe('Projects pagination', () => {
  it('should show pagination component with correct total', async () => {
    // Mock API to return paginated response
    global.fetch = vi.fn().mockResolvedValue({
      json: () => Promise.resolve({
        items: [{ id: '1', name: 'Test' }],
        total: 1,
        page: 1,
        page_size: 20
      })
    })

    const wrapper = mount(Projects)
    await flushPromises()

    const pagination = wrapper.findComponent({ name: 'ElPagination' })
    expect(pagination.exists()).toBe(true)
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd frontend && npm test -- --run src/tests/Projects.pagination.test.ts`
Expected: FAIL — pagination component not found

- [ ] **Step 3: 改造 Projects.vue**

改动要点：
1. 引入 `usePagination`
2. 替换 `projects` ref 为 `items`（来自 composable）
3. 替换 `filteredProjects` 为服务端过滤（保留前端搜索过滤）
4. 添加 `el-pagination` 组件

```vue
<template>
  <!-- 保留原有结构，替换 table data -->
  <el-table :data="items" stripe style="width:100%">
  <!-- ... -->
  </el-table>

  <el-pagination
    background
    layout="total, sizes, prev, pager, next, jumper"
    :total="total"
    :page-size="pageSize"
    :current-page="page"
    @size-change="onSizeChange"
    @current-change="onPageChange"
    style="margin-top: 20px; justify-content: flex-end;"
  />
</template>

<script setup lang="ts">
import { usePagination } from '@/composables/usePagination'

const { items, total, page, pageSize, loading, fetchPage, onPageChange, onSizeChange } = usePagination(
  async (p, ps) => {
    const res = await projectsApi.list(p, ps)
    return { items: res.data.items, total: res.data.total }
  }
)

// 搜索时重置到第一页
watch(search, () => fetchPage(1))

onMounted(() => fetchPage(1))
</script>
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd frontend && npm test -- --run src/tests/Projects.pagination.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add frontend/src/views/Projects.vue frontend/src/tests/Projects.pagination.test.ts
git commit -m "feat: add pagination to Projects page"
```

---

## Task 7: 前端 — 改造 Iterations.vue

**Files:**
- Modify: `frontend/src/views/Iterations.vue`

- [ ] **Step 1: 写失败测试**

```typescript
// frontend/src/tests/Iterations.pagination.test.ts (新文件)
import { flushPromises } from '@vue/test-utils'

describe('Iterations pagination', () => {
  it('should render pagination component', async () => {
    // Mock API...
    const wrapper = mount(Iterations)
    await flushPromises()
    expect(wrapper.findComponent({ name: 'ElPagination' }).exists()).toBe(true)
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd frontend && npm test -- --run src/tests/Iterations.pagination.test.ts`
Expected: FAIL

- [ ] **Step 3: 改造 Iterations.vue**

改造模式同 Task 6：
1. 引入 `usePagination`
2. `iterations` ref → `items` (composable)
3. `filteredIterations` 改为仅保留前端搜索/筛选，分页由服务端处理
4. 添加 `el-pagination`
5. 搜索/筛选变化时 `fetchPage(1)`

- [ ] **Step 4: 运行测试确认通过**

Run: `cd frontend && npm test -- --run src/tests/Iterations.pagination.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add frontend/src/views/Iterations.vue frontend/src/tests/Iterations.pagination.test.ts
git commit -m "feat: add pagination to Iterations page"
```

---

## Task 8: 前端 — 改造 Requirements.vue

**Files:**
- Modify: `frontend/src/views/Requirements.vue`

- [ ] **Step 1: 写失败测试**

```typescript
// frontend/src/tests/Requirements.pagination.test.ts (新文件)
import { flushPromises } from '@vue/test-utils'

describe('Requirements pagination', () => {
  it('should render pagination component with correct total', async () => {
    // Mock API returning 45 items, page 1 of 20
    global.fetch = vi.fn().mockResolvedValue({
      json: () => Promise.resolve({
        items: Array(20).fill({ id: '1', title: 'Test' }),
        total: 45,
        page: 1,
        page_size: 20
      })
    })

    const wrapper = mount(Requirements)
    await flushPromises()
    const pagination = wrapper.findComponent({ name: 'ElPagination' })
    expect(pagination.exists()).toBe(true)
    expect(pagination.props('total')).toBe(45)
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd frontend && npm test -- --run src/tests/Requirements.pagination.test.ts`
Expected: FAIL

- [ ] **Step 3: 改造 Requirements.vue**

改造模式同 Task 6/7：
1. 引入 `usePagination`
2. `requirements` ref → `items` (composable)
3. `displayedRequirements` computed 改为服务端分页
4. 添加 `el-pagination`
5. filter 变化时 `fetchPage(1)`

- [ ] **Step 4: 运行测试确认通过**

Run: `cd frontend && npm test -- --run src/tests/Requirements.pagination.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add frontend/src/views/Requirements.vue frontend/src/tests/Requirements.pagination.test.ts
git commit -m "feat: add pagination to Requirements page"
```

---

## Task 9: 前端 — 同步 API 层适配

**Files:**
- Modify: `frontend/src/api/index.ts`

**Interfaces:**
- Consumes: `PaginatedResponse` 格式
- Produces: API 调用适配器

- [ ] **Step 1: 写失败测试**

```typescript
// frontend/src/tests/api.pagination.test.ts (新文件)
import { describe, it, expect } from 'vitest'
import { projectsApi, iterationsApi, requirementsApi } from '@/api'

describe('API pagination', () => {
  it('should return paginated response shape', async () => {
    // Mock global fetch...
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd frontend && npm test -- --run src/tests/api.pagination.test.ts`
Expected: FAIL

- [ ] **Step 3: 适配 API 层**

检查 `frontend/src/api/index.ts`，确认各 API 方法能处理新的 `{ items: [], total, page, page_size }` 响应格式。若需要调整，确保 `.data` 访问返回正确结构。

- [ ] **Step 4: 运行测试确认通过**

Run: `cd frontend && npm test -- --run src/tests/api.pagination.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add frontend/src/api/index.ts frontend/src/tests/api.pagination.test.ts
git commit -m "feat: adapt API layer for paginated responses"
```
